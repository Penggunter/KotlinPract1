<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Определение стиля для текста ФИО -->
    <style name="StudentNameTextStyle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Определение стиля для текста номера группы -->
    <style name="GroupNameTextStyle" parent="TextAppearance.AppCompat.Subhead">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>

</resources>